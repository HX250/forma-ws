name: Build and deploy NestJS and Angular app to Azure - Forma

on:
  push:
    branches:
      - main

env:
  API_END_POINT: ${{ secrets.API_END_POINT }}

jobs:
  install-dependencies:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Upload node_modules
        uses: actions/upload-artifact@v4
        with:
          name: node-modules
          path: node_modules
          retention-days: 1

  test-backend:
    runs-on: ubuntu-latest
    needs: install-dependencies
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Download node_modules
        uses: actions/download-artifact@v4
        with:
          name: node-modules
          path: node_modules

      - name: Generate Prisma Client
        run: ./node_modules/.bin/prisma generate --schema=./apps/api/prisma/schema.prisma

      - name: Run API unit tests
        run: ./node_modules/.bin/nx test api

  test-frontend:
    runs-on: ubuntu-latest
    needs: install-dependencies
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Download node_modules
        uses: actions/download-artifact@v4
        with:
          name: node-modules
          path: node_modules

      - name: Run Web unit tests
        run: ./node_modules/.bin/nx test web

  database-migration-and-generate:
    runs-on: ubuntu-latest
    needs: [test-backend, test-frontend]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Check for schema changes
        id: check-schema
        run: |
          if git diff --name-only HEAD^ HEAD | grep "apps/api/prisma/schema.prisma"; then
            echo "schema_changed=true" >> $GITHUB_OUTPUT
          else
            echo "schema_changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Download node_modules
        if: steps.check-schema.outputs.schema_changed == 'true'
        uses: actions/download-artifact@v4
        with:
          name: node-modules
          path: node_modules

      - name: Run Prisma Migrations
        if: steps.check-schema.outputs.schema_changed == 'true'
        run: ./node_modules/.bin/prisma migrate deploy --schema=./apps/api/prisma/schema.prisma
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

      - name: Generate Prisma Client
        if: steps.check-schema.outputs.schema_changed == 'true'
        run: ./node_modules/.bin/prisma generate --schema=./apps/api/prisma/schema.prisma
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

  build-and-deploy-backend:
    runs-on: ubuntu-latest
    needs: database-migration-and-generate
    permissions:
      id-token: write
      contents: read

    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Download node_modules
        uses: actions/download-artifact@v4
        with:
          name: node-modules
          path: node_modules

      - name: Generate Prisma Client
        run: ./node_modules/.bin/prisma generate --schema=./apps/api/prisma/schema.prisma
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

      - name: Build NestJS API with Nx
        run: ./node_modules/.bin/nx build api --prod

      - name: Prepare deployment structure
        run: |
          mkdir -p deploy
          cp -r dist/apps/api/* deploy/
          cp package*.json deploy/
          cp -r apps/api/prisma deploy/

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: 'Deploy to Azure Web App'
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'forma-ws-api'
          slot-name: 'Production'
          package: ./deploy
        env:
          NODE_ENV: 'production'
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          JWT_EXPIRES_IN: ${{ secrets.JWT_EXPIRES_IN }}
          JWT_REFRESH_SECRET: ${{ secrets.JWT_REFRESH_SECRET }}
          JWT_REFRESH_EXPIRES_IN: ${{ secrets.JWT_REFRESH_EXPIRES_IN }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          MAIL_FROM: ${{ secrets.MAIL_FROM }}

  build-and-deploy-frontend:
    runs-on: ubuntu-latest
    needs: database-migration-and-generate

    permissions:
      id-token: write
      contents: read

    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Download node_modules
        uses: actions/download-artifact@v4
        with:
          name: node-modules
          path: node_modules

      - name: Set environment variables
        run: node apps/web/scripts/set-env.js
        env:
          API_END_POINT: ${{ secrets.API_END_POINT }}

      - name: Build Angular app with Nx
        run: ./node_modules/.bin/nx build web --prod

      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: 'upload'
          app_location: './dist/apps/web/browser'
          api_location: ''
          output_location: ''
          skip_app_build: true
